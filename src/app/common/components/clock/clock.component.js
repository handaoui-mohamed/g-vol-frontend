import template from './clock.html';
import controller from './clock.controller';
import './clock.scss';

let clockComponent = {
	bindings: {},
	template,
	controller,
	controllerAs: "clock"
};

export default clockComponent;
