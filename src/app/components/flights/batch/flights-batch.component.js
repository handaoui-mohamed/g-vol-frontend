import template from './flights-batch.html';
import controller from './flights-batch.controller';
import './flights-batch.scss';

let flightsBatchComponent = {
  bindings: {},
  template,
  controller,
  controllerAs: 'flbVm'
};

export default flightsBatchComponent;
